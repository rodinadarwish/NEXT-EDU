import Navbar from '../components/Navbar'
import styles from '../styles/register.module.css'
const register = () => {
    return (
    
     <div className={`${styles.back}`}>
        <Navbar/>
        <div className={`container`} >#register here </div>
    </div>
    
    );
}
 
export default register;